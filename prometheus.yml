global:
  scrape_interval: 5s

scrape_configs:
  - job_name: car-dealership-app
    metrics_path: /actuator/prometheus
    basic_auth:
      username: prometheus
      password_file: /tmp/prometheus-scrape-password
    static_configs:
      - targets:
          - app:8080
